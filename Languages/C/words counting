#include<stdio.h>
#include<string.h>
int main()
{
	int i ,word=0;
	char str[100];
	printf("Enter the string\n");
	gets(str);
	for(i=0;str[i]!='\0';i++)
	{
		if(str[i]==' ')
		{
			word++;
		}
	}
	printf("The number of word are %d",word+1);
	
}
